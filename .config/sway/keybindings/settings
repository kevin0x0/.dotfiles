include common

# Settings
mode "settings" bindsym {
  Escape mode "default"
  $mod+q mode "default"
  $mod+Shift+q mode "default"

  # Reload the configuration file
  $mod+c reload; mode "default"
}
bindsym $mod+x mode "settings"

# vim: ft=swayconfig
