#!/bin/sh

PROMPT="Workspace: "
[ "$LANG" = zh_CN.UTF-8 ] && PROMPT="移动到工作区："

workspace=$(swaymsg -rt get_workspaces \
  | jq -r '.[].name' \
  | menu-wrapper dmenu -p "$PROMPT")

swaymsg -- move container to workspace "$workspace"
