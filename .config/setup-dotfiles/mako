if [ "${XDG_STATE_HOME:-$HOME/.local/state}" != ~/.local/state ]; then
  error_exit "XDG_STATE_HOME must be set to ~/.local/state"
fi

COLORSCHEME_DIR=${XDG_STATE_HOME:-$HOME/.local/state}/colorscheme/mako/colorscheme
mkdir -p "$(dirname -- "$COLORSCHEME_DIR")"
ln -sf -- "$COLORSCHEME_DIR/catppuccin-mocha" "$COLORSCHEME_DIR"

# vim: ft=sh
